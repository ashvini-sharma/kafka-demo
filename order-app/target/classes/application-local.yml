server:
  port: 8081

spring:
  kafka:
    bootstrap-servers: localhost:9092

logging:
  level:
    root: INFO
    com.example.orderapp: DEBUG

  file:
    name: logs/order-app/order.log

  pattern:
    file: "%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n"